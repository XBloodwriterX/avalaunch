{
  "name": "avalaunch",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "node --experimental-strip-types --test tests/**/*.test.ts && node tests/cdp-adblock-test.mjs",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "typescript": "5.7.3",
    "vite": "6.2.1"
  }
}
